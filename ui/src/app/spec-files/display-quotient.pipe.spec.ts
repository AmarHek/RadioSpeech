import { DisplayQuotientPipe } from '../pipes/display-quotient.pipe';

describe('DisplayQuotientPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayQuotientPipe();
    expect(pipe).toBeTruthy();
  });
});
